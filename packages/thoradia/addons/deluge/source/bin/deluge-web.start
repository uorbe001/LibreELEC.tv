#!/bin/sh

. /etc/profile
oe_setup_addon service.deluge

if [ -z "$dl_nice" ]; then
  dl_nice="0"
fi

LD_LIBRARY_PATH="$ADDON_DIR/deluge" \
nice -n "$dl_nice" \
python "$ADDON_DIR/deluge/deluge-web" -c "$ADDON_HOME"
